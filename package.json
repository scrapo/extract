{
	"name": "extract",
	"description": "extract",
	"repository": "scrapo/extract",
	"version": "1.0.0",
	"main": "extract.js",
	"scripts": {
		"test": "mocha --ui tdd test"
	},
	"dependencies": {
		"each": "scrapo/each",
		"select": "scrapo/select",
		"cheerio": "^0.22.0"
	},
	"devDependencies": {
		"mocha": "^3.2.0"
	}
}